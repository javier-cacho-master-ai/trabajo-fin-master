SELECT
  gaia.source_id
  ,gaia.ra
  ,gaia.dec
  ,gaia.phot_bp_mean_mag
  ,gaia.phot_rp_mean_mag

  -- Razón Señal a Ruido BP/RP
  ,gaia.phot_bp_mean_flux_over_error
  ,gaia.phot_rp_mean_flux_over_error

  -- Ruido
  ,gaia.astrometric_excess_noise

  -- Color
  ,gaia.bp_rp

  -- Temperatura efectiva
  ,gaia.teff_gspphot

  -- Gravedad superficial
  ,gaia.logg_gspphot

  -- Metalicidad
  ,gaia.mh_gspphot

  -- Macroturbulencia
  ,gaia.vbroad

  -- Abundancia de elementos alfa
  ,ap.alphafe_gspspec

  ,xmatch.original_ext_source_id
  ,xmatch.angular_distance
FROM
  gaiadr3.gaia_source AS gaia
  JOIN gaiadr3.astrophysical_parameters AS ap
    ON gaia.source_id = ap.source_id
  JOIN gaiadr3.sdssdr13_best_neighbour AS xmatch
    ON gaia.source_id = xmatch.source_id
WHERE
  xmatch.original_ext_source_id IN ({sdss_ids})
  AND gaia.has_xp_continuous = 'true'
  AND xmatch.angular_distance <= 2
