SELECT
  s.bestObjID
  ,s.specObjID
  ,s.plate
  ,s.mjd
  ,s.fiberID
  ,s.z AS redshift
FROM
  SpecObj AS s
WHERE
  s.sciencePrimary = 1
  AND s.plate BETWEEN {plate_start} AND {plate_end}
ORDER BY
  s.specObjID
